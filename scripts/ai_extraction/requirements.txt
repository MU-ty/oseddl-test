# ============================================================================
# AI Agent 活动提取系统 - 核心依赖
# ============================================================================

# 核心依赖 (必需)
requests>=2.31.0              # HTTP请求
beautifulsoup4>=4.12.0        # HTML解析
pydantic>=2.5.0               # 数据验证
pydantic-settings>=2.1.0      # 配置管理
pyyaml>=6.0                   # YAML处理
python-dotenv>=1.0.0          # 环境变量

# 异步支持
aiohttp>=3.9.0                # 异步HTTP
# asyncio-timeout已内置于Python 3.10+，不需要单独安装

# 文档解析
playwright>=1.40.0            # Web自动化
pillow>=10.1.0                # 图像处理

# ============================================================================
# AI提供商 - 可选 (选择一个或多个)
# ============================================================================

# GitHub Models (推荐 - 免费) ✅
# 无需额外依赖，系统默认支持

# OpenAI API (可选 - 付费) 
# 仅在使用OpenAI时需要
openai>=1.3.0
langchain>=0.1.0
langchain-openai>=0.0.2

# ============================================================================
# 可选功能 - 如需启用，取消注释下面的行
# ============================================================================

# OCR支持 (图像文字识别)
# pytesseract>=0.3.10
# python-magic>=0.4.27

# 二维码识别
# pyzbar>=0.1.9
# qrcode>=7.4.2

# PDF处理
# PyPDF2>=3.0.1
# pypdf>=3.0.1
