name: Simple Test Workflow

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    
    if: |
      contains(github.event.comment.body, '@bot extract') ||
      contains(github.event.comment.body, '@bot 提取')
    
    steps:
      - name: Log trigger
        run: |
          echo "✅ 工作流已触发!"
          echo "事件类型: ${{ github.event.action }}"
          echo "评论内容: ${{ github.event.comment.body }}"
          echo "Issue 号: ${{ github.event.issue.number }}"
      
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Reply to issue
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '✅ 工作流已收到您的命令！\n\n🎉 系统已成功触发并处理了您的请求。'
            });
